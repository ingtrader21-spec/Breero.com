import { EmailWorkspace } from "@/components/portal/email-workspace";

export const metadata = { title: "Tenant email administration" };

export default function AdminEmailPage() {
  return <EmailWorkspace/>;
}
