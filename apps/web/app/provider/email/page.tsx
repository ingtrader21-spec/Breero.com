import { EmailWorkspace } from "@/components/portal/email-workspace";

export const metadata = { title: "Provider email workspace" };

export default function ProviderEmailPage() {
  return <EmailWorkspace/>;
}
