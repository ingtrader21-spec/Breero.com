import { SupportCaseWorkspace } from "@/components/support/support-case-workspace";

export const metadata = { title: "Support cases" };

export default function SupportCasesPage() {
  return <SupportCaseWorkspace />;
}
